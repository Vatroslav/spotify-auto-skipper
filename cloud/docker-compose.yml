services:
  skipper:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - skipper_data:/app/data
    restart: unless-stopped

volumes:
  skipper_data:
